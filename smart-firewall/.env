# Network interface for capture (CHANGE THIS)
CAPTURE_INTERFACE=eth0

# Flow timeout in seconds
FLOW_TIMEOUT=300

# ML Model settings
ML_MODEL_TYPE=cnn
CONFIDENCE_THRESHOLD=0.8

# Action settings
ENABLE_BLOCKING=true  # Set to true only when ready

# Data directories (automatically created)
DATA_DIR=./data